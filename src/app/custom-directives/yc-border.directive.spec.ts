import { YcBorderDirective } from './yc-border.directive';

describe('YcBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new YcBorderDirective();
    expect(directive).toBeTruthy();
  });
});
